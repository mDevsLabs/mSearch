#!/bin/sh
set -e

if [ "$1" = "configure" ] || [ "$1" = "abort-upgrade" ]; then
    update-alternatives --install /usr/bin/x-www-browser \
        x-www-browser /opt/mSearch/min 80
    chown root /opt/mSearch/chrome-sandbox
    chmod 4755 /opt/mSearch/chrome-sandbox
    echo "Run sudo xdg-settings set default-web-browser min.desktop && sudo update-alternatives --config x-www-browser to set mSearch as your default browser."
    ln -sf /opt/mSearch/min /usr/bin/min
fi
